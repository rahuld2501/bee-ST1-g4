const express = require('express');
const router = express.Router();
const reviewController = require('../controllers/reviewController');

// Review CRUD routes (if needed separately)
// ...

module.exports = router;